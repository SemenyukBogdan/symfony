<form method="post" action="{{ path('app_book_reviews_delete', {'id': book_review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_review.id) }}">
    <button class="btn">Delete</button>
</form>
