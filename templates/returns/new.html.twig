{% extends 'base.html.twig' %}

{% block title %}New Returns{% endblock %}

{% block body %}
    <h1>Create new Returns</h1>

    {{ include('returns/_form.html.twig') }}

    <a href="{{ path('app_returns_index') }}">back to list</a>
{% endblock %}
