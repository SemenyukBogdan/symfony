{% extends 'base.html.twig' %}

{% block title %}Returns{% endblock %}

{% block body %}
    <h1>Returns</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ return.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_returns_index') }}">back to list</a>

    <a href="{{ path('app_returns_edit', {'id': return.id}) }}">edit</a>

    {{ include('returns/_delete_form.html.twig') }}
{% endblock %}
