{% extends 'base.html.twig' %}

{% block title %}Edit Returns{% endblock %}

{% block body %}
    <h1>Edit Returns</h1>

    {{ include('returns/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_returns_index') }}">back to list</a>

    {{ include('returns/_delete_form.html.twig') }}
{% endblock %}
